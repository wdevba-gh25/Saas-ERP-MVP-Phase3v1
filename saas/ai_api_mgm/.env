# === ERP-AI Python Gateway ===
# Port you will run the gateway on (see README)
PORT=8009

# Where the existing LLM_Client FastAPI is running:
AI_PROVIDER_BASE=http://localhost:8008

# Optional: if you want this gateway to call your .NET ERP for raw data later
ERP_API_BASE=http://localhost:5240/api

# CORS: your Vite dev origin
FRONTEND_ORIGIN=http://localhost:5173

# MY LOCAL LAN MISTRAL 7B
LOCAL_LLM_URL=http://localhost:8001/v1/completions

# Local DB, static 1433, trust local cert (simplest dev setup)
MSSQL_CONN_STR=Driver={ODBC Driver 17 for SQL Server};Server=localhost,1433;Database=SaaSMvp;Uid=erp_gateway_ro;Pwd=L4l1t4M0p@--zz;Encrypt=no;TrustServerCertificate=yes
